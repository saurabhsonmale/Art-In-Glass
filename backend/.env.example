# Replace with your actual MongoDB Atlas connection string
# Get this from: MongoDB Atlas -> Connect -> Connect your application -> Driver: Python
# Example: mongodb+srv://username:password@cluster0.abc123.mongodb.net/resin_art_db?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@YOUR_CLUSTER_NAME.mongodb.net/resin_art_db?retryWrites=true&w=majority
JWT_SECRET_KEY=your_jwt_secret_key_here_change_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
PORT=8000
ENVIRONMENT=production

# Live Render URL
PUBLIC_BASE_URL=https://art-in-glass.onrender.com
CORS_ORIGINS=*

# MongoDB Atlas → Network Access must allow Render to connect.
# Recommended for live APIs: 0.0.0.0/0 (allow from anywhere)
# Also allow these CIDRs if your host / proxy uses them:
#   74.220.49.0/24
#   74.220.57.0/24
