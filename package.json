{
  "name": "art-in-glass",
  "private": true,
  "description": "Monorepo root. The live API is Python under /backend — do not use yarn start for Render. See render.yaml.",
  "scripts": {
    "start": "node ./scripts/render-start.js"
  }
}
